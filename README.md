## Configuration de l'environnement (Windows)

Après avoir cloné le projet, créez votre environnement virtuel local **sans ajouter `.venv` au repo** :

```cmd
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt